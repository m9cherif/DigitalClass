PORT=3000
JWT_SECRET=change-me-to-a-long-random-string
NODE_ENV=development

# Optional TURN relay for the live classroom. Without it, peers behind strict
# school firewalls may fail to connect (plain STUN is used by default).
# TURN_URL=turn:turn.example.com:3478
# TURN_USER=
# TURN_PASS=
